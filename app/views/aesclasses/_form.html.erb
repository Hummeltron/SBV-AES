<%= form_for(aesclass) do |f| %>
  <% if aesclass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aesclass.errors.count, "error") %> prohibited this aesclass from being saved:</h2>

      <ul>
      <% aesclass.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
