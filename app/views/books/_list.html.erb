<table>
	<thead>
		<tr>
			<th>Titel</th>
			<th>Preis</th>
			<th>Kaufbuch</th>
			<th>Isbn</th>
			<th colspan="3"></th>
		</tr>
	</thead>
	<tbody>
		<% @books.each do |book| %>
			<tr>
				<td><%= book.label %></td>
				<td><%= book.price %></td>
				<td><%= book.topay %></td>
				<td><%= book.isbn %></td>
				<td>
					<div>
						<%= link_to 'Anzeigen', book %>
						<%= link_to 'Bearbeiten', edit_book_path(book) %>
						<%= link_to 'Löschen', book, method: :delete, data: { confirm: 'Sind sie sicher?' } %>
					</div>
				</td>
			</tr>
		<% end %>
	</tbody>
</table>
