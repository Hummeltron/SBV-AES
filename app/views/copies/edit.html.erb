<h1>Editing Copy</h1>

<%= render 'form' %>

<div class="btn-group">
  <%= link_to 'Show', @copy, :class => "button" %>
  <%= link_to 'Back', student_path(@copy.student), :class => "button" %>
</div>
