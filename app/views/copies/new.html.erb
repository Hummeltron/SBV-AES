<div class="panel panel-default">
	<div class="panel-heading clearfix">
		<h3 class="panel-title" style="display:inline">Neue Kopien</h3>
	</div>
	<div class="panel-body">
    	<%= form_for(@copy) do |f| %>
  			<div class="field">
    			<%= f.label "Buch" %>
    			<%= f.collection_select :book_id, Book.all, :id, :label, prompt: true %>
  			</div>
			<div class="field">
    			<%= f.label "Anzahl" %>
    			<%= number_field_tag :count %>
    		</div>
  			<div class="field">
    			<%= f.label "Kaufbuch" %>
    			<%= f.check_box :topay %>
  			</div>

  			<div class="actions">
    			<%= f.submit "Speichern" %>
  			</div>
		<% end %>
  </div>
	</div>
	<div class="panel-footer">
		<div class="btn-group">
			<%= link_to 'Zurück', copies_path, :class => "button" %>
		</div>	
	</div>
</div>