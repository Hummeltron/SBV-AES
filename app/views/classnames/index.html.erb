<p id="notice"><%= notice %></p>

<div class="panel panel-default">
  <div class="panel-heading clearfix">
    <h3 class="panel-title" style="display:inline">Klassen</h3>
    <%= link_to 'Neue Klasse', new_classname_path, :class => "btn btn-default pull-right" %>
  </div>
  <% if @classnames.present? %>
	  <%= render 'list' %>
  <% else %>
	  <p>Keine Klassen vorhanden</p>
  <% end %>
</div>
